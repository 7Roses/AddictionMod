if AddictionMod == nil then AddictionMod = {} end -- cause it can be loaded before or after the header is been called.

AddictionMod.config = {}













-- MODDING API FOR THIS MOD
-- if you want to add another medication or addiction to the game

local function addAddiction(properties)
  if properties == nil then
    
  end
end